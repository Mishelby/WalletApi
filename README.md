# 💰 Система управления банковскими кошельками

Проект представляет собой сервис для управления банковскими кошельками с использованием:

- **Spring Boot**
- **PostgreSQL**
- **Liquibase** (для управления миграциями базы данных)

Для удобного запуска и разработки используется **Docker** и **Docker Compose**.

---

### 1️⃣ Предварительные требования

- JDK 17
- Docker и Docker Compose
- Gradle (для локальной сборки)

---

### 2️⃣ Стек технологий

- **Java 17 / Spring Boot**
- **PostgreSQL 17**
- **Liquibase**
- **Docker / Docker Compose**

---

### 3️⃣ Структура Docker Compose

Сервисы:

- `postgres-dev` – контейнер с PostgreSQL для разработки
- `app` – Spring Boot приложение
- `adminer` – веб-интерфейс для работы с базой (опционально)

💡 Все настройки приложения и базы данных можно менять через `.env` без пересборки контейнеров.

---

### 4️⃣ Файл `.env`

Пример:

```env
APP_PORT=8080
DB_NAME=walletdb
DB_USER=walletuser
DB_PASSWORD=walletpass
DB_PORT=5432
DDL_AUTO=update
SWAGGER_ENABLED=true
```
---

### 5️⃣ Сборка JAR файла

Перед запуском контейнеров убедитесь, что JAR-файл приложения собран:

```bash
./gradlew clean bootJar
```

После этого в build/libs/ появится файл WalletAPI-0.0.1-SNAPSHOT.jar.

### 6️⃣ Сборка Docker образов

Если используется multi-stage Dockerfile (сборка JAR внутри контейнера):

```bash
docker-compose build
```

Для быстрой разработки с локальным JAR можно монтировать его через volume (см. пункт 7).

### 7️⃣ Быстрая разработка (использование локального JAR)

Чтобы не пересобирать Docker-образ при каждом изменении кода:

Соберите локальный JAR:

```bash
./gradlew clean bootJar
```

В docker-compose.yml добавьте volume:

services:
app:
volumes:
- ./build/libs/WalletAPI-0.0.1-SNAPSHOT.jar:/app/app.jar


Запустите контейнеры:

```bash
docker-compose up
```

✅ Контейнер будет использовать локальный JAR, и любые изменения JAR автоматически применяются.

### 8️⃣ Запуск контейнеров

Обычный запуск:

```bash
docker-compose up
```

Запуск в фоне:

```bash
docker-compose up -d
```

Приложение подождёт, пока PostgreSQL станет доступен (используется healthcheck и depends_on).

### 9️⃣ Остановка контейнеров

Обычная остановка:

```bash
docker-compose down
```

Удаление всех данных PostgreSQL:

```bash
docker-compose down -v
```

⚠️ Внимание: при использовании -v все данные базы будут удалены!

### 🔟 Полезные ссылки

Adminer: http://localhost:8082
– веб-интерфейс для работы с PostgreSQL

Swagger UI: http://localhost:8080/swagger-ui.html
(если включён)

openAPI: http://localhost:8080/v3/api-docs
